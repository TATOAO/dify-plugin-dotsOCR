# Dots.OCR Plugin Dependencies
# All dependencies can be packaged into Dify Plugin via requirements.txt

# OpenAI client for vLLM API calls
openai>=1.0.0

# Image processing library
pillow>=9.0.0

# PDF processing library (PyMuPDF)
pymupdf>=1.23.0

# HTTP requests library (fallback option)
requests>=2.28.0
